{
  "name": "gd-school-management-system",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web,app -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix App\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && npm run build --prefix App",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix App",
    "cap:sync": "npm run cap:sync --prefix App"
  },
  "devDependencies": { "concurrently": "^9.1.2" }
}
