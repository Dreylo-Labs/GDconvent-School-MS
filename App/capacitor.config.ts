import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.edu.gdconvent.parent',
  appName: 'G.D. Convent School',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};

export default config;
